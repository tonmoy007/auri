# 🕯️ Auri — Implementation Plan

> **For Hermes:** A mobile app where teammates anonymously record "confessions" (feedback, gripes, appreciations, ideas) via an AI-driven STT/TTS agent with voice modulation for anonymity, inside a stunning interactive 3D confessional booth. Start/End recording, AI processes it, voice-changed audio forwarded anonymously or deleted.

**Goal:** Build **Auri** — an anonymous AI-driven confession booth mobile app with immersive 3D UI, STT/TTS agent, real-time voice modulation, and forward-or-delete action flow.

**Architecture:** React Native (Expo) mobile app with React Three Fiber for the 3D confessional booth environment. Backend FastAPI server handles STT (Whisper), AI processing (LLM), TTS, voice conversion (RVC/SoX), and anonymous message forwarding. All PII and original voice stripped server-side — the app itself tracks nothing.

**Tech Stack:**
| Layer | Choice |
|-------|--------|
| Mobile | React Native + Expo |
| 3D UI | React Three Fiber + drei + Three.js |
| Backend | FastAPI + WebSocket (for streaming) |
| STT | OpenAI Whisper (or local faster-whisper) |
| TTS | Edge-TTS (free, beautiful voices) |
| **Voice Change** | **SoX audio processing (pitch/formant) + RVC (AI voice conversion)** |
| AI Agent | Any LLM (GPT-4o / Claude) via API |
| DB | PostgreSQL + pgcrypto (PII-safe) |
| Auth | Anonymous tokens only (no identity stored) |

---

## Resume Protocol

Tracking file: `~/.hermes/plans/2026-07-16_143000-confession-booth-tracking.json`

## UX Flow

```
[Tap "Enter Auri"]
    ↓
[3D Booth opens — dim candlelit interior, rotating camera]
    ↓
[AI voice greets you warmly, asks what's on your mind]
    ↓
[Pick Voice Mask — Warm | Robotic | Ethereal | Deep | Random]
    ↓
[User speaks — voice is modulated in real-time, STT streams, booth glows with voice waves]
    ↓
[End Recording — AI processes, categorizes, de-identifies]
    ↓
[Shows transcription ↔ AI summary side by side]
    ↓
[Anonymity choice: Fully Blind | "Someone in your team" context]
    ↓
[Three options: Send Anonymously (with voice-masked audio) | Forward to Someone | Delete]
    ↓
[If Forward: AI suggests best dept/person from manual list]
    ↓
[Moderation queue — designated moderator reviews flagged items]
    ↓
[Delivery via Telegram bot → recipient gets anonymous message + voice-masked audio clip]
    ↓
[Confirmation — booth fades, candle extinguishes]
```

---

## Architecture Overview

```
┌──────────────────────────────────────────────────┐
│              React Native (Expo) App             │
│  ┌─────────────────────┐  ┌───────────────────┐  │
│  │ 3D Booth (R3F/Three)│  │ Screen Stack       │  │
│  │ ─ Rotating booth     │  │ Home → Booth →     │  │
│  │ ─ Candle + glow FX   │  │ Review → Anonymity │  │
│  │ ─ Voice wave rings   │  │ → Forward → Confirm │  │
│  │ ─ Animated door       │  │                   │  │
│  └─────────┬───────────┘  └────────┬──────────┘  │
│            │                        │              │
│         ┌──▼────────────────────────▼──┐          │
│         │    API Layer (fetch/ws)       │          │
│         └───────────────┬──────────────┘          │
└─────────────────────────┼────────────────────────┘
                          │ HTTPS / WSS
┌─────────────────────────▼────────────────────────┐
│              FastAPI Backend                      │
│  ┌──────────┐ ┌───────────────┐ ┌──────┐ ┌─────────────────┐  │
│  │ Whisper  │ │ Voice Mod     │ │ LLM  │ │ Edge-TTS (free) │  │
│  │ (STT)   │ │ SoX + RVC     │ │Agent │ │ (TTS voice)     │  │
│  └────┬─────┘ └───────┬───────┘ └──┬───┘ └────────┬────────┘  │
│       │              │                 │           │
│  ┌────▼──────────────▼─────────────────▼────────┐ │
│  │          PostgreSQL + pgcrypto               │ │
│  │  (confessions encrypted at rest, no PII)     │ │
│  └──────────────────────────────────────────────┘ │
│  ┌──────────────────────┬───────────────────────┐ │
│  │  Telegram Bot        │  Moderation Queue     │ │
│  │  (python-telegram-   │  (flagged → moderator │ │
│  │   bot, webhook mode) │   approve/reject)     │ │
│  └──────────┬───────────┴───────────┬───────────┘ │
└─────────────┼───────────────────────┼─────────────┘
              │                       │
    ┌─────────▼─────────┐   ┌────────▼─────────┐
    │  Recipient gets   │   │  Moderator sees  │
    │  anonymous DM     │   │  queue in Telegram│
    │  via Telegram Bot │   │  approve/reject   │
    └───────────────────┘   └──────────────────┘
```

---

## Implementation Phases

### Phase 1: Project Scaffolding & 3D Booth (2 days)

| Task | Status | Description | Files |
|------|--------|-------------|-------|
| 1.1 | ✅ completed | Init Expo project with R3F + drei + expo-av | `mobile/package.json`, `mobile/app.json`, `mobile/tsconfig.json` |
| 1.2 | ✅ completed | Build 3D confessional booth scene — rotating walls, wood texture, dim lighting. **Correction (2026-07-22): the scene itself was fine, but `confession/[id].tsx` rendered `<ConfessionBooth>` directly, never wrapped in `<ThreeCanvas>` (the only component that mounts an actual R3F `<Canvas>`) — so every `useFrame` call inside it (Candle, Particles) crashed at runtime with "Hooks can only be used within the Canvas component!" This is not web-specific; it would have crashed on iOS/Android identically. Found by actually running the app (Expo web export + Playwright), not by review — nothing in lint/typecheck catches this class of bug. Fixed by wrapping `<ConfessionBooth>` in `<ThreeCanvas>` on the booth screen.** **Correction 2 (2026-07-22, real Android emulator via `expo start --android`): the fix above only made the crash *identical* across platforms — it never actually ran natively until now. Found 4 stacked native-only crashes web export never surfaced (web R3F uses a different renderer path): (1) `expo-gl` was never in `package.json` at all — `@react-three/fiber`'s native renderer hard-requires it; Metro couldn't even bundle. Fixed: added `expo-gl@~15.0.5`. (2) drei's `<Text>` (troika-three-text) calls `document`, which doesn't exist on native — crashed with "Property 'document' doesn't exist". Fixed: removed the decorative in-scene "AURI" label mesh (the label is already shown as real 2D UI text elsewhere). (3) drei's `<Environment preset>` (image-based lighting) crashed three.js's `PMREMGenerator` with "WeakMap key must be an Object" — expo-gl's WebGL shim doesn't fully back what PMREM needs. Fixed: removed it; the scene already has explicit ambient/directional/point lights plus the candle's own point light, so IBL reflections weren't load-bearing. Also proactively fixed a 3rd issue the same sweep exposed: three.js's precision auto-detection calls `gl.getShaderPrecisionFormat()`, unimplemented by expo-gl, crashing `WebGLCapabilities` — fixed by forcing `precision: 'lowp'` in the Canvas `gl` config, the only value that skips that call. **A 4th crash remains open, tracked as 1.8 below** — see that row.** | `mobile/src/components/ConfessionBooth.tsx`, `mobile/src/components/ThreeCanvas.tsx`, `mobile/src/app/confession/[id].tsx`, `mobile/package.json` |
| 1.3 | ✅ completed | Add candle flame animation + ambient glow (GLSL shader or sprite-based) | `mobile/src/components/Candle.tsx` |
| 1.4 | ⏳ pending | Booth door open/close animation + floating dust particles | `mobile/src/components/ThreeBooth/Door.tsx`, `.../Particles.tsx` |
| 1.5 | ⏳ pending | Voice-responsive ring visualization — rings pulse with audio amplitude | `mobile/src/components/ThreeBooth/VoiceRings.tsx` |
| 1.6 | ⏳ pending | Camera orbit animation — slow drift, pulls in on record, pulls out on end | `mobile/src/components/ThreeBooth/BoothCamera.ts` |
| 1.7 | 🔄 in_progress | Booth entry/exit transition (fade-in from black, cross-fade to review) | `mobile/src/app/index.tsx` |
| 1.8 | ⏳ pending | **Blocking, discovered 2026-07-22 on real Android emulator**: after fixing 1.2's three native crashes, a 4th remains — three.js shader/uniform setup throws "Cannot read property 'trim' of undefined" (`onFirstUse`/`getUniforms`/`setProgram`), i.e. `gl.getShaderInfoLog`/`getProgramInfoLog`-class calls returning `undefined` instead of a string. Confirmed **not** a `three` version issue: pinned `three` down to `~0.160.0` and the identical crash reproduced, so reverted that pin (no benefit, not worth the unverified downgrade). Root cause is an `expo-gl` WebGL-shim gap independent of the `three` version. The booth still cannot fully render on native until this is fixed — booth screen shows a red-box crash past this point. Needs either a working `expo-gl` version bump/downgrade, or isolating which specific material/shader feature triggers the bad shim call. | `mobile/src/components/ThreeCanvas.tsx`, `mobile/package.json` |

### Phase 2: Audio Recording, Voice Modulation & STT Pipeline (2.5 days)

| Task | Status | Description | Files |
|------|--------|-------------|-------|
| 2.1 | ✅ completed | Implement audio recording with Expo AV — start/end buttons, waveform preview | `mobile/src/hooks/useAudioRecorder.ts`, `mobile/src/components/RecordButton.tsx` |
| 2.2 | ✅ completed | Voice mask selection UI — Warm / Robotic / Ethereal / Deep / Random with preview | `mobile/src/components/VoiceMaskSelector.tsx` |
| 2.3 | ✅ completed | Real-time voice modulation engine — SoX pitch-shift + formant manipulation on recorded audio | `backend/app/services/voice_mod.py` |
| 2.4 | ⏳ pending | AI voice conversion (optional RVC) — for hyper-realistic voice anonymization | `backend/app/services/rvc_converter.py` |
| 2.5 | ✅ completed | Whisper transcription service. **Correction (2026-07-20): previously marked completed on 2026-07-17 on the strength of the service file existing — it was never exposed over HTTP, so no client could ever turn a recording into a transcript.** Fixed: `POST /api/v1/stt` (multipart upload, rate-limited, deletes temp file after transcription). | `backend/app/services/stt.py`, `backend/app/api/v1/stt.py` |
| 2.6 | 🔄 in_progress | Upload audio with progress indicator + retry logic | `mobile/src/hooks/useAudioRecorder.ts` |

### Phase 3: AI Agent Processing (2 days)

| Task | Status | Description | Files |
|------|--------|-------------|-------|
| 3.1 | ✅ completed | LLM agent — categorize (gripe/praise/idea/confession), extract de-identified entities, assign sentiment | `backend/app/services/llm.py`, `backend/app/services/deidentify.py` |
| 3.2 | ✅ completed | Confession creation flow — transcription → AI summary/category, folded into `POST /api/v1/confessions`. **Correction (2026-07-18): previously marked completed on 2026-07-17 without verifying `ai_summary`/`category` were actually populated — they weren't (`categorize`/`summarize` were never called). Fixed and now genuinely complete.** | `backend/app/api/v1/confessions.py` |
| 3.3 | ✅ completed | Expose TTS as an HTTP endpoint — `POST /api/v1/tts`, streams WAV, deletes file after send | `backend/app/api/v1/tts.py`, `backend/app/services/tts.py` |
| 3.4 | ✅ completed | Review screen — transcription vs AI summary, edit before forwarding | `mobile/src/app/review.tsx` |

### Phase 4: Telegram Bot, Forward/Delete & Anonymous Delivery (2.5 days)

| Task | Status | Description | Files |
|------|--------|-------------|-------|
| 4.1 | ✅ completed | Telegram bot setup — python-telegram-bot, `/start` `/help` `/confess` `/forward` commands | `bot/main.py`, `bot/config.py` |
| 4.2 | ✅ completed | Forward UI (2026-07-22) — new `forward/[id].tsx` screen: fetches `GET /api/v1/departments`, lets the user pick one, calls `POST /confessions/{id}/forward`. Real gap found while scoping: `review.tsx` already had a "Forward Anonymously" button, but it only ever called `POST /confessions` directly — the `forward` endpoint (and `recipient_dept`) was never reachable from the app at all. Wired: when the anonymity toggle is off ("Someone in your team"), Review now creates the confession then routes here to pick a department; toggle on stays fully blind (unchanged, no department). Department-lookup-by-person is out of scope — backend only supports department-level routing. | `mobile/src/app/forward/[id].tsx`, `mobile/src/app/review.tsx`, `mobile/src/app/_layout.tsx` |
| 4.3 | ⏳ pending | Anonymity choice UI — toggle between "Fully blind" vs "Someone in your team" with visual preview. Partially addressed as a side effect of 4.2 (the existing Review toggle's labels/copy now correctly reflect this choice and drive real behavior instead of being inert) but the plan's original ask was a dedicated screen with a visual preview, which this is not — leaving pending. | `mobile/src/app/anonymity.tsx` |
| 4.4 | ✅ completed | `POST /api/v1/confessions` — save confession, generate anonymous delivery token; `POST .../forward` endpoint | `backend/app/api/v1/confessions.py`, `backend/app/models/confession.py` |
| 4.5 | 🔄 in_progress | Bot delivery flow — deliver confession to recipient via Telegram DM (anonymous relay) | `bot/main.py` (`handle_confession_message`) |
| 4.6 | ✅ completed | Moderation queue. Backend: `LLMService.moderate()` flags confessions at creation (fails closed on error, runs on the raw transcript — see 3.2's correction note above for why). `GET/POST /api/v1/moderation/*` (queue/approve/reject), secured by `MODERATION_API_KEY`. Bot (2026-07-19): `bot/moderation_handlers.py` polls the queue on a repeating job, posts flagged items to `MODERATOR_TELEGRAM_CHAT_ID` with inline Approve/Reject buttons wired to the backend endpoints. | `backend/app/api/v1/moderation.py`, `backend/app/services/llm.py`, `bot/moderation_handlers.py` |
| 4.7 | ✅ completed | Delete flow (2026-07-22) — new `delete-confirmation.tsx` screen: explicit confirm/cancel step before the destructive delete fires (previously `review.tsx`'s Delete button called `DELETE` immediately with zero confirmation — a real accidental-delete risk), then plays a candle-extinguish animation (`Candle`'s flame/glow/light fade to zero via a new `extinguishProgress` prop) while the request is in flight. | `mobile/src/app/delete-confirmation.tsx`, `mobile/src/app/review.tsx`, `mobile/src/components/Candle.tsx`, `mobile/src/components/ConfessionBooth.tsx` |
| 4.8 | ⏳ pending | Home screen with history (past confessions, delivery status) | `mobile/src/app/home.tsx` |

### Phase 5: Polish & 3D Environments (1 day)

| Task | Status | Description | Files |
|------|--------|-------------|-------|
| 5.1 | ⏳ pending | Multiple ambient environments — classic booth, forest glade, rooftop at night | `mobile/src/components/environments/` |
| 5.2 | ✅ completed | Haptic feedback — `useHaptics()` wraps `expo-haptics`, wired into record start/stop, forward success, delete confirm, mask/environment selection, anonymity toggle. **Wiring verified via lint/typecheck only — actual haptic feel on hardware not confirmed, no device/simulator available.** | `mobile/src/hooks/useHaptics.ts` |
| 5.3 | ⏳ pending | Sound design — booth ambiance, candle crackle, door creak, confirm chime. **Genuinely blocked, not skipped**: this needs real audio asset files, which nothing in this session can generate or source. Leaving pending rather than faking placeholder files. | `mobile/assets/audio/` |
| 5.4 | ✅ completed | Loading states — candle pulses faster/brighter while `status === 'processing'` (`Candle.tsx` → `ConfessionBooth.tsx`); `ShimmerText` (opacity pulse loop) on the "Anonymizing…" status label and the "Submitting…" forward button. **Verified via lint/typecheck only — no simulator to confirm the animation looks right.** | `mobile/src/components/LoadingStates.tsx` |

### Phase 6: Data Lifecycle, Observability & Release Readiness (2 days)

> Newly discovered — real gaps not covered by Phases 1–5, found by auditing the codebase against this plan on 2026-07-17.

| Task | Status | Description | Files |
|------|--------|-------------|-------|
| 6.1 | ✅ completed | Audio/data retention job — audio was already never persisted (STT/TTS delete temp files immediately). `purge_stale_confessions()` hard-deletes `forwarded`/`deleted` confession rows past `RETENTION_HOURS` (default 24h); `pending`/`flagged` never touched. Not run in-process — invoke via `python -m app.services.retention` on cron/k8s CronJob. | `backend/app/services/retention.py` |
| 6.2 | ✅ completed | Error tracking & metrics — `init_sentry()` (no-op without `SENTRY_DSN`) + `GET /metrics` (Prometheus format, request count + latency histogram via middleware) | `backend/app/observability.py` |
| 6.3 | 🔄 in_progress | `eas.json` build profiles (development/preview/production) — done. **Store metadata (descriptions, screenshots, privacy-policy URL) intentionally NOT fabricated** — that's marketing/product copy requiring human input and a real hosted privacy policy page (doesn't exist yet), not something to invent as placeholder text. | `mobile/eas.json` |
| 6.4 | 🔄 in_progress | Detox config + navigation smoke test scaffolded (`.detoxrc.js`, `e2e/appLaunch.e2e.js`). **Never executed in this environment — no Xcode/Android SDK/simulator available to build or run against.** `detox`/`jest` deliberately NOT added to `package.json` — installing them pulled in 14 high + 2 medium severity CVEs (old `bunyan`/`node-ipc` in detox's tree) that failed Trivy; not worth blocking CI for a dependency I can't even exercise here. Whoever picks this up on a real machine runs `npm install --save-dev detox jest` themselves (gets current, non-flagged versions) before `npm run e2e:test:ios`. Scoped to navigation only, not the full record→transcribe→forward path (needs mocked/injected audio input, not yet built). | `mobile/.detoxrc.js`, `mobile/e2e/` |
| 6.5 | ✅ completed | Privacy/anonymity security review. Found + fixed: bot's `error_handler` logged the full raw `Update` object (message content) on any failure — fixed to log only `update_id`. Found + documented (deliberate tradeoff, not fixed): `moderate()` sends the raw transcript to the external LLM, not the de-identified one — see doc for why. DB rows and moderation-queue Telegram payloads confirmed clean (de-identified only). | `docs/privacy-review.md`, `bot/main.py` |
| 6.6 | ✅ completed | Moderator & department directory config — `MODERATOR_TELEGRAM_CHAT_ID` + `DEPARTMENTS` settings, `GET /api/v1/departments`, forward endpoint now validates against the configured list (422 if unknown) | `backend/app/config.py`, `backend/app/api/v1/departments.py`, `backend/app/api/v1/confessions.py` |

### Phase 7: LiveKit Real-Time Voice Pipeline (scoping — 2026-07-20)

> Scoped from a possibilities analysis of the `livekit-agents`/`livekit-simulations` skills against Auri's actual code. Real finding: `mobile/src/hooks/useWebSocket.ts` is a fully-built real-time client (partial transcript, summary, mask-status, error event dispatch) waiting on a backend that was never built — `backend/app/main.py`'s `/ws/confession` is a stub that just echoes an ack. The plan's original UX Flow (AI greets → live back-and-forth → real-time voice masking) was never actually deliverable by the current batch record→upload→transcribe pipeline. LiveKit Agents is a strong fit for closing that gap. **Not started — planning/scoping only, per explicit instruction.** Do not implement without first satisfying 7.1 (the `livekit-agents` skill is a hard requirement here, not a suggestion: verify every API against live docs/MCP, never from memory).

| Task | Status | Description | Files |
|------|--------|-------------|-------|
| 7.1 | ⏳ pending | Verify current LiveKit Agents SDK (Python) API surface via MCP/`docs.livekit.io` before writing any code — method signatures, import paths, and Cloud vs self-hosted setup all drift fast per the skill's own freshness rules | n/a (verification gate) |
| 7.2 | ⏳ pending | Provision LiveKit Cloud project; add `LIVEKIT_URL`/`LIVEKIT_API_KEY`/`LIVEKIT_API_SECRET` to backend config + `.env.example`; add an `agent` service stub to `docker-compose.yml` | `backend/app/config.py`, `.env.example`, `docker-compose.yml` |
| 7.3 | ⏳ pending | Minimal LiveKit Agent (Python worker, separate deployment unit from the FastAPI app) implementing the live booth conversation — greet, listen, respond — replacing the `WS_EVENTS` protocol `useWebSocket.ts` already expects | `agent/` (new service, mirrors `bot/`'s structure) |
| 7.4 | ⏳ pending | Real-time voice masking in the agent's audio pipeline — live equivalent of `backend/app/services/voice_mod.py`'s SoX/RVC masking, applied mid-conversation instead of post-hoc on a completed file | `agent/voice_pipeline.py` |
| 7.5 | ⏳ pending | Live moderation guardrail — run the `LLMService.moderate()` equivalent per-utterance during the conversation, not only once at confession submit (directly closes the class of gap found and fixed 2026-07-18: a self-harm case that `moderate()` missed when it only ran once, after de-identify had already corrupted its input) | `agent/moderation_guard.py` |
| 7.6 | ⏳ pending | Mobile: integrate LiveKit's Expo/React Native client SDK into the booth screen, replacing `useAudioRecorder`'s record-then-upload flow with a live room join for the conversational path (REST `/stt`/`/tts` stay as the async fallback) | `mobile/src/app/confession/[id].tsx`, `mobile/src/hooks/useLiveKitRoom.ts` (new) |
| 7.7 | ⏳ pending | Agent behavior tests — mandatory per the `livekit-agents` skill: basic conversation flow, tool invocation (if any), error handling, at least one test before the implementation counts as done | `agent/tests/` |
| 7.8 | ⏳ pending | Generate + run simulation scenarios via the `livekit-simulations` skill's flow (risk checklist covering the moderation guardrail, refusal/out-of-scope cases) before shipping to production | scenarios generated locally, not committed as agent code |

### Phase 8: Expo App Settings (2026-07-20)

> User-requested: a single Settings section in the Expo app surfacing every user-facing preference that currently exists only as a hardcoded default or an inline, unreachable action. Two real correctness gaps found while scoping this: (1) `confession/[id].tsx` reset `voiceMask`/`environment` to hardcoded defaults on every visit — no persistence existed to surface as a setting; (2) the device-token reset/rotate logic lived inline in `review.tsx` with its own module-level cache, which would have silently diverged from a second copy pasted into the settings screen — extracted to a shared module instead of duplicated.

| Task | Status | Description | Files |
|------|--------|-------------|-------|
| 8.1 | ✅ completed | Extract device-identity logic (`getSessionDeviceToken`, `hashDeviceToken`, new `resetDeviceToken`) out of `review.tsx` into a shared module — a second inline copy in the settings screen would have run its own in-memory cache, diverging from `review.tsx`'s within the same app session | `mobile/src/lib/deviceToken.ts` |
| 8.2 | ✅ completed | `useSettings` hook — persists default voice mask + default booth environment via `expo-secure-store`, loads on mount, exposes setters that write through | `mobile/src/hooks/useSettings.ts` |
| 8.3 | ✅ completed | Settings screen — Voice & Booth (default mask/environment), Privacy & Identity (anonymous ID status, reset with confirm), About (version, tagline) | `mobile/src/app/settings.tsx` |
| 8.4 | ✅ completed | Wire `confession/[id].tsx` to seed its live `voiceMask`/`environment` state from the persisted settings defaults instead of hardcoded `'ethereal'`/`'classic'` | `mobile/src/app/confession/[id].tsx` |
| 8.5 | ✅ completed | Settings entry point (gear button, top-right) on the home screen; register the route in the root stack | `mobile/src/app/index.tsx`, `mobile/src/app/_layout.tsx` |

---

## 3D UI Vision (Interactive Booth)

```
                   ┌──────────────────────┐
                   │   Booth Interior (3D) │
                   │                      │
                   │    ╔══════════╗      │
                   │    ║  🔥🕯️   ║      │ ← Candle flame (animated)
                   │    ╚══════════╝      │
                   │                      │
                   │  ◯ ◯ ◯ ◯ ◯ ◯      │ ← Voice wave rings
                   │                      │    (pulse with audio)
                   │    "Speak freely,   │
                   │     this is sacred" │ ← AI voice greets
                   │                      │
                   │  ┌────────────────┐ │
                   │  │  🎙️ RECORD    │ │ ← Glass-button with ripple
                   │  └────────────────┘ │
                   │                      │
                   │  🌙 Candlelit wood  │ ← Warm ambient lighting
                   │  ✨ Dust particles  │ ← Floating motes
                   └──────────────────────┘
```

**Visual Moodboard:**
- Dark mahogany wood textures (warm browns)
- Single flickering candle as primary light source
- Floating dust motes in volumetric light beam
- Voice visualized as concentric glowing rings
- Record button = frosted glass orb that glows red during recording
- Forward = paper crane that flies away animation
- Delete = candle extinguishes + smoke wisps

---

## Data Privacy Design

```
┌─────────────────────────────────────────────────┐
│                 Privacy Layer                     │
├─────────────────────────────────────────────────┤
│ • No user accounts — anonymous device tokens     │
│ • Audio deleted from server after transcription  │
│ • Transcript encrypted at rest (pgcrypto)        │
│ • LLM prompt strips all PII before DB write      │
| • Recipient sees only: "A teammate shared: ..."  │
| • Forwarded messages routed through blind relay  │
| • Optional: self-destruct after 24h read         │
| • **Dual anonymity mode:**                        │
|   → Fully blind (no context at all)              │
|   → "Someone in your team" (team context shown)  │
|   → User picks at send-time                      │
└─────────────────────────────────────────────────┘
```

---

## ✅ Resolved Decisions

| Question | Decision |
|----------|----------|
| **Recipient discovery** | Manual department list — admin-managed in-app (no LDAP) |
| **Anonymity spectrum** | **Both** — user chooses at send-time: fully blind OR "someone in your team" context |
| **Notification channel** | **Telegram bot** — each confession delivered via anonymous bot message |
| **Moderation** | Yes — flagged content queued to **designated moderator user(s)** via Telegram for review |
| **Environment variety** | Classic booth + optional forest glade + rooftop night |

---

## Testing & Validation

- `npm run test` — Jest for components + hooks
- `pytest backend/tests/` — FastAPI endpoint tests
- Manual: Full flow walkthrough on iOS Simulator + Android Emulator
- E2E: Detox for critical paths (record → transcribe → forward)

## Build & Deploy

- **Backend:** Docker Compose → Railway / Fly.io
- **Mobile:** EAS Build → TestFlight (iOS) + Internal Track (Android)
- **3D assets:** glTF/GLB files bundled in app, no network fetch
